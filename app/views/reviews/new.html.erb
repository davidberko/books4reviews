<%= form_with(model: book, local: true) do |form| %>

  <% form.hidden_field :user_id %>
  <% form.hidden_field :book_id %>

  <%= form.check_box_tag :claim %>

  <%= form.submit_tag :submit %>

<% end %>
